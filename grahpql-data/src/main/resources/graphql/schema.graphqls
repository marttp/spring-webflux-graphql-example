type Query {
    customers: [Customer]
    customerById(id: ID): Customer
    customersByName(name: String): [Customer]
}

type Customer {
    id: ID
    name: String
    profile: Profile
}

type Profile {
    id: ID
}